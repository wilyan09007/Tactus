# Changelog — Tactus

> **Current state lives in [`truth.md`](truth.md), not here.** This log is the chronological record of the project, **including the abandoned pre-pivot era** (3-pillar Experience/Express; Raspberry Pi + ESP32 + MAX98357A; 16/14-channel). Older entries are kept as history — do not read them as the current build (as-built = laptop → 2× Vantec → 6× SK473/PAM8403 → 12× KHD drivers, LEARN + PLAY).

## 2026-06-20 — Markerless fretboard registration + guitar digital twin (no taped marker)
- **`software/ai/vision/fretboard.py`** — markerless registration from the 12-TET fret-spacing law (`x_n = 1−2^(−n/12)`): the guitar is its own ruler, so no fiducial is needed. Fits the fretboard homography from the guitar's own geometry and validates by reprojecting the fret grid onto the real wires. Synthetic front-cam self-test passes (~1.3px median reproj, recovers signed `d`). This becomes the PRIMARY registration path; the ArUco marker is demoted to an OPTIONAL ground-truth validator (`cad/aruco/`).
- **`software/ai/vision/calib_serve.py` + `calibrate.html`** — localhost guitar digital-twin capture. Hold the neck at ~6 angles, click the 4 neck corners on each frozen frame; the fret grid overlays live (in-browser 4-pt homography) so you SEE it snap onto your real frets — paperless proof. Saves keyframes to `data/calib/<guitar>/`.
- **`software/ai/vision/twin.py`** — builds `twin.json` from the keyframes: fret-law intrinsics (pose-invariant) + a validated homography per captured angle + corner-reproj residual + board color. The "memorize the guitar once, solve the pose per frame" split.
- **`capture.html`** — on-screen **Framing guide** (nut→fret 7, whole fretting hand, lock camera + mic, training=demo) so the operator doesn't memorize the protocol; the **Pose-variation block** now prompts the finger/wrist/neck-angle change each pass (logged as `pose_variant`).
- **`cad/aruco/`** — `generate_marker.py` + print sheet (4×4_50 id 0), kept ONLY as the optional offline validation rig.

## 2026-06-20 — Localhost capture app (synced A/V + maximal metadata + live QC)
- **`software/ai/capture/serve.py` + `capture.html`** — stdlib localhost server + one Chrome page. Records **synced webcam+mic per run** (lossless WAV + webm, same `getUserMedia` stream) and writes straight to `data/raw/<session>/<player>/` with a maximal-metadata `manifest.jsonl`. Run: `python3 software/ai/capture/serve.py` → open the printed `http://localhost:8765`. End-to-end save round-trip tested.
- **Quality guards:** disables browser DSP (echo/noise/AGC off) so the buzz band survives; live input-level meter + **red CLIP alarm**; per-run silent/too-short checks; coverage grid + clip/silent/fail counts; per-run **download fallback** if the server is down (no take lost).
- **Maximal metadata** per run (for the harness loop): string/fret/finger/class/placement/pluck(+variant)/chord/pass/room, beat-grid times, duration, peak dBFS + clip/silent flags, sample-rate, video res/fps, mic+cam device labels, file paths/bytes, marker-present, sync note, app version.
- **Blocks:** core grid · pose-variation · pluck-sweep · muted · choked · arpeggiated chords · strummed · natural holdout. Prompt = the label; interleaved order kills session drift.
- `docs/24 §10` rewritten localhost-first (`record_conductor.py` + QuickTime kept as the zero-dep fallback); `software/ai/capture/README.md` added; `.gitignore` now also drops `*.webm`.

## 2026-06-20 — Data-collection: frets 1–6 scope lock, last-mile↔data framing, Day-1 recorder
- **SCOPE LOCK (D6): frets 1–6 only, 1:1 fret→zone** (6 fret-zone motors — we record/coach only what we can render). Resolves the "fret span beyond 6" open thread and supersedes the eng-review V5 neck-spanning ask. Reconciled across `truth.md §3.2`, `docs/24` (§0b/§3 + decisions), and `docs/23` (breadth pass).
- **`docs/24 §0b` — "what the data actually trains: the two last miles."** Vision (MediaPipe + ArUco/homography) does the bulk for free; capture only pins two residuals — Stage 1 (occluded pose → string,fret,finger) and Stage 2 (buzz B + d → pressure/cause). One recording trains both; the prompt is the label. This is *why* ~1,000 takes suffice vs ~100× for a raw-pixel model.
- **`docs/24 §10` + `software/ai/capture/record_conductor.py` — Day-1, zero-build recording.** Terminal conductor (stdlib only) prompts each interleaved run and writes the manifest (prompt = label); QuickTime captures synced A/V in one file. Run: `python3 software/ai/capture/record_conductor.py --player <name> --passes 3`. The localhost/align-mode harness is the next build.
- **`.gitignore`** — ignore raw capture media (`data/raw/**/*.wav|mp4|mov`), keep `manifest.jsonl` tracked.

## 2026-06-19 — Refinement pass on the as-built rig (enclosure CAD + build kinks + LEARN UI)
Aligned to Aritro's as-purchased docs (`docs/00`–`13`, laptop → 3× Vantec → 7× SK473/PAM8403 → 14× 40 mm). Added, on branch `refine/enclosure-build-interface`:
- **`cad/` — printable enclosure** for everything-but-vest-and-subwoofers, sized to the **FlashForge Adventurer 5M (220³, PETG)**. Parametric OpenSCAD + **pre-rendered STLs** (all verified ≤ 220 mm): `tactus_box` (base+lid: 3 Vantec + 7 amp bricks + Pi 5, vented, **strain-relief comb** + zip-tie floor), `tactus_power_cradle` (Anker 737 / 10-port hub, removable + vented), `actuator_puck` (turns a 40 mm cone speaker into a body tactor — the coupling fix).
- **`docs/15-build-refinements.md`** — the kinks that bite: **PAM8403 is BTL → never common the "−" outputs**; the **3-Vantec/ALSA enumeration trap** (bind by USB path, verify CM6206 channel order); wire-gauge split (heavy 18 AWG on the power bus, light stranded on the body); cone-speaker **coupling**; corrected the Mode-A "50 W rail" claim (it's a 12 V hub w/ per-port limits → run at felt level); the exhaustive **what-connects-to-what** table; single-channel-first solder order.
- **`docs/16-learn-interface.md` + `web/index.html`** — LEARN UI refined to the **real 14-channel string/fret body map** (6 chest + 6 forearm + 2 back), the 3-fix correction panel, note highway, and a **runnable mock** (no backend) that demonstrates the WebSocket `frame`/`note`/`error`/`reference` contract.
- Component dims researched (Anker 737 155.7×54.6×49.5/630 g; Vantec 100×58×26; SK473 brick ~55×35×25; Pi 5 ~26 mm clearance) to size the box. Resolved the enclosure open-question in `docs/13`.
- **Scope per Aditya: LEARN + PLAY are the product now; EXPERIENCE/Express/Body-Synth dropped** — already consistent with the as-built guitar-coaching direction.
- **Pi cut from scope (laptop-only).** Drops the QNX dependency; CAD standoffs removed (`include_pi=false`), base STL re-rendered. **`docs/17-ai-rigor.md`** added — the deep audio+vision **fusion → high-granularity (string,fret,finger,quality)** argument, the 3-axis targeted feedback, the eval/ablation rigor moves, and how the **interface showcases the AI**; awards reoriented to **Most-Technical / Best-UI-UX / Anthropic / Accessibility / CV / Redis**.
- **`docs/15 §10`** — laptop connection + power topology: the **one-powered-hub-does-both** path (one wall plug + one USB cable to the laptop + laptop on its own power) and the Anker cordless variant. Wire reality corrected to **18 AWG only** (BOM's 22 AWG line is stale); `actuator_puck` notch widened to fit it and re-rendered.

## 2026-06-19 — Hardware corrected after live inventory cross-check (physics)
- **Dropped the heavy bass shakers from the wearable.** Live check: Micro Center's in-store shakers are **BST-2 ≈ 1180 g** and **AuraSound ≈ 1700 g** — built for couches/floors, **not wearable** (would sag/tear the vest, hurt). Directly answers Aditya's weight/physics question.
- **Wearable actuators = 8× light exciters** (Dayton **DAEX25 ≈ 111 g**, or TT25-8 Puck ≈ 170 g). **Micro Center does NOT carry exciters → order DAEX25 on Amazon (Sat delivery);** if MC has TT25 Pucks on the shelf, buy 8 there instead.
- **Final channel architecture (all in-stock, all 5 V):** **Pi 5 → 2× ESP32 (2 I2S buses each) → 8× MAX98357A → 8 exciters.** 2 amps per I2S bus (L/R strap) → 4 ch/ESP32 → 8 total. **No Teensy** (ships 5–7 days, not needed), **no 24 V brick / TPA3116** (~1.8 W/exciter against skin is plenty). Simpler, lighter, touch-safe.
- **`03` rewritten with the corrected FINAL BUY LIST** + phone-confirm steps (MC 403s web stock; the 833 number is unverified — use the Google Maps listing). `02` finished-product + channel sections corrected. ~$350 MC + $104 Amazon + $30 Target; guitar from Aditya's friend.

## 2026-06-19 — LEARN re-architected: vision + haptic closed-loop guitar coaching (per Aditya)
- **Dropped the 3 practice modes** (Recognize/Anticipate cut). LEARN is now **one coherent loop** = *"Rocksmith for Deaf players."*
- **Core idea:** a hearing player closes the learning loop by ear (play → hear the gap → fix); a Deaf player can't. We close it with **(a) haptic ground-truth** — feel the correct version on the vest, and feel *your version vs correct back-to-back* so the error is tangible — **and (b) vision** — a camera + **VLM (Claude vision)** watches the fretting hand and gives the **exact physical change**, highlighting the wrong finger.
- **The loop:** feel the target → play the real guitar (mic + camera) → on a wrong segment, **rewind → replay correct haptics (+ feel-the-difference) → VLM shows the physical fix → one instruction → re-loop.**
- **The physical-correction engine (novel core):** fuses mic (F0 / onset-offset / harmonic-to-noise+decay) + vision (MediaPipe Hands + fretboard + Claude vision) + the reference (tab/MIDI fingering) → **exactly 3 advice categories: wrong note · incorrect duration · wrong pressure.**
- **GUITAR ONLY** for the hackathon (easiest to analyze visually). Generalizes later.
- **Redis promoted to a headline sponsor:** on-device **local memory → offline practice anywhere** (songs + fingering + calibration + progress). Baked into the core loop, not a cache.
- **Anthropic flagship deepened:** **Claude's vision is the coaching brain** (frame + target + error → physical fix + finger highlight) — a real multimodal integration. Deepgram = hands-free ("rewind"/"slower"). 
- **Rewrote `13`**; updated `00` (LEARN pillar), `05` (now the real-time PLAY engine that LEARN builds on; guitar lead), `06` (Anthropic vision + Redis offline), `07` (the correction demo beat), `08` (workstream D = the vision loop), README.

## 2026-06-19 — Vest-primary form factor + LEARN web app added to scope (per Aditya)
- **Form factor flipped to VEST-PRIMARY** (`00`/`02`/`11`). The chest+back vest is now the **hero** (build first) — central torso = strongest *emotional* feeling (bass in the chest), full tonotopic canvas, carries a whole song alone. Optional **Piece 2 = a glove/hands** (fingertip tactors for the Play tuner + IMU/EMG for Express gesture). Clean split: **vest = feel/learn, glove = play/express.** (Reversed the earlier sleeve-first call.)
- **Three use cases locked as EXPERIENCE · LEARN · PLAY** (`00`). "Express" (create music) now lives inside **Experience** as its flagship active mode. *(Flagged for confirmation — if the team wants Express as its own top-level pillar, easy to split back out.)*
- **NEW `13_LEARN_WEB_AND_VISUALIZATION.md`** — a website tuned to the haptics that **teaches a Deaf person a song** via synced **visual reinforcement + haptic feedback**. Real (non-decorative) value: (A) Guitar-Hero "Haptic Roll" that teaches the body-mapping + anticipation, (B) live body-map, (C) structure-timeline + lyrics, (D) the **note→deconstruction→haptics pipeline visualization** (the explainability showpiece). Practice modes (Follow/Recognize/Anticipate) with real scoring. Grounded in Deaf-music-learning evidence (Tranchant/Hopkins/González-Garrido). **Parallelizable:** built against a fixed WebSocket JSON contract so one person owns the web front-end independently of hardware. Drives Best UI/UX + Most-Technical + Anthropic(tutor)/Arize.
- **Build plan:** workstream **D** is now the LEARN web app; **lock the WebSocket schema Saturday AM** (the seam for parallel work).

## 2026-06-19 — Direction changes (per Aditya): reframe + remove the score
- **⛔ Removed the score-based architecture.** Dropped the symbolic "JSON haptic-score that Claude composes." A generated score is an abstraction that *weakens* rigor (invites "is the AI just making it up?"). Replaced with a **direct, continuous signal→skin transcription** — measurable frame-by-frame fidelity to the source, the same *class* as a **cochlear-implant processor / vocoder**. genAI stays where it's defensible: the **learned audio→haptic model**, the **gesture→sound mapping** (Express), and **Claude as an accessible music tutor** (Learn). Saving/sharing = the **actual rendered haptic signal** (HJIF signal mode), not an AI score. Updated `00, 04, 06, 07, 08, 09, 12`, README.
- **Reframed the whole product as EXPRESS · PLAY · LEARN** (one engine, three pillars). *Express* = gesture→sound→felt-back (creative loop). *Play* = real instrument → real-time tuner/rhythm haptics on the wrists. *Learn* = feel the **real songs everyone listens to** → community/belonging (the emotional core). The shared engine = wideband hardware + the direct transcription + the learned model. Updated North Star + README + sponsors.
- **Answered: body placement** (`11`/`00`) — wearable "tactile cochlea": back 2×4 tonotopic ladder (low→lower back, high→shoulders), lumbar/abdomen bass shakers, sternum vocals, **wrists for the PLAY tuner feedback** (hands busy), ankles for kick, fingertip for fine pitch; **not the face**.
- **Answered: PLAY latency** (`05`/`00`) — real-time, **~20–50 ms** on the live paths (which use lightweight causal F0 + voice-coil actuators, **not** the heavy model). Imperceptible for tuning; vibration lags audio; minimized via voice-coil (not ERM), small buffers, wired-not-Bluetooth, beat prediction. Heavy model/separation only runs offline on known songs.
- **Ponytail:** user installs it as a per-user Claude Code plugin (their choice); nothing committed to the repo.


## 2026-06-19 — Full pivot: Hearth → Tactus
- **Pivoted** from *Hearth* (contactless sleep/seizure guardian) to **Tactus**: a wideband, multi-node haptic wearable that lets Deaf people feel music (rhythm, pitch contour, timbre, spatially) and learn instruments by touch.
- **Why:** the team wanted a build where the *rigor and complexity of the haptics themselves* are the wow — a research-grade audio→haptic system, plus a genAI "favorite song → haptic" experience and a live, emotional demo.
- **Locked:** the North Star, the honest claim discipline (a *new sensory language*, not "hearing"; Deaf-culture-respectful, not a "cure"), and the working name *Tactus*.
- **Launched** a 5-front deep-research pass to ground the build: (1) tactile psychophysics & illusions, (2) actuator/driver hardware engineering, (3) Microcenter Santa Clara / Target same-day sourcing, (4) research-grade audio→haptic ML/DSP, (5) instrument-learning + sponsor strategy.
- **Carry-over from Hearth:** sponsor landscape (QNX, Deepgram, Anthropic, Sentry, Arize, Fetch, Redis), the quality bar (honest-but-stunning claims + live emotional demo), and likely on-hand hardware (Raspberry Pi, USB mic/speaker, ESP32s from the booth).
- **Open item:** Aditya's salvage-parts list not yet received — plan is salvage-agnostic with a slot to fold them in.

### Research pass landed — full doc set written (later 2026-06-19)
- All 5 research agents returned; synthesized into the cited doc set (`00`–`08` + raw `REF_` briefs).
- **Key unlocks:** (1) **Microcenter Santa Clara stocks voice-coil bass shakers in-store** (Dayton BST-2, AuraSound) — wideband actuators buyable today, not just buzzers. (2) Wideband requires **voice-coil exciters + bass shakers driven by class-D audio amps** (NOT ERM/LRA/PWM) — the literal "not a buzzer" differentiator. (3) Skin = ~5 octaves, ~15–20 distinguishable frequencies, ~3–5 cm torso acuity ⟹ **place-code pitch + exploit phantom/funneling illusions + lean on rhythm**. (4) ML core = **distill the DSP psychohaptic mapping into a multi-channel neural net** (MERT→TCN/GRU) with a perceptual+discriminability loss; multi-channel spatial generation is the novel paper-grade contribution (HapticGen etc. are single-channel). (5) **Claude = haptic choreographer** emitting a JSON score → MPEG-I HJIF. (6) Sponsors form one coherent system: **Claude composes (Anthropic) → uAgents orchestrate (Fetch) → QNX real-time on Pi → Deepgram voice → Arize eval**; main track **Ddoski's World** (social impact) + **Ddoski's Lab** depth.
- **Locked scope:** FeelStream (hero sensation), the learned audio→haptic model, PlayByTouch (instrument-by-touch), Claude the Haptic Composer.
- **Honest framing locked:** a *new sensory/artistic language*, designed **with** the Deaf community — never "making Deaf people hear." Recruit a Deaf advisor (prize multiplier + the right thing).
- **Top build risk flagged:** actuator coupling/backing (surface-dependent exciter response) — prototype first thing Saturday.

### Added creative-expression direction (2026-06-19, per Aditya)
- New doc `09_CREATIVE_EXPRESSION.md`: the **grand-prize inversion** — Deaf people don't just *feel* music, they **create** it natively in touch and feel their own creation in real time (the **Body Synth** closed loop). Forward audio→haptic model run *backward* (gesture→sound+felt-back-haptics), emotion as the cross-modal invariant. Three modes (Body Synth / Tactile Composer / Cross-Modal Art Object). Grounded in Deaf art culture (Christine Sun Kim, De'VIA, Visual Vernacular, Deaf Gain). Staged as the visionary peak *after* consumption proves it works; **first on the cut list so it never risks the core.**
- Added Build #5 to North Star scope; threaded into build plan (Sunday stretch) + demo (the mic-drop performance beat).
- **Sourcing updated:** borrow the guitar / rent the violin (Music & Arts ~$20–30/mo) instead of buying — instrument cost ~$0–30 vs ~$80–140.

### Second research pass — sourcing consolidation + 4 new docs (2026-06-19, per Aditya)
- **Sourcing consolidated to a clean 2-STOP run** (`03` rewritten). Micro Center + Guitar Center are both on Stevens Creek Blvd in Santa Clara, 1.7 mi apart, both open till 9pm → one southbound trip. Violin from **Allegro Music, Fremont** (⚠️ **Music & Arts has NO local store — earlier note was wrong**). Optional TENS unit at Target Fremont. **Best in-store amp = Adafruit MAX98357A (I2S, no DAC); don't drive for amps; Amazon same-day unreliable Fri evening.** Phone-ahead checklist + "don't drive to" list (Fry's/HSC/Excess all closed/non-local).
- **`10_GESTURE_PIPELINE.md`** — feel music through body expression: **ESP32 + BNO055 (orientation) + MyoWare (effort) → OSC → Wekinator regression → granular synth → audio + haptic felt-back**, closed loop <~10 ms (jitter worse than fixed delay). Mi.Mu/Wekinator/Hunt-Wanderley mapping; pitch→location not felt-freq; LLM as offline structure-oracle; RAVE/DDSP as real-time neural-timbre bet. MediaPipe-hands laptop = $0 fallback.
- **`11_STIMULATION_MODALITIES.md`** — body-placement map (wearable Model Human Cochlea: back 2×4 + sternum + lumbar bass + wrist/ankle); **pneumatic squeeze = best-evidenced non-vibration add** (dynamics); **EMS "guide-the-hand" PlayByTouch** (PossessedHand/Lopes precedent) via FDA TENS + openEMSstim attenuate-only, **limb-only, consented, kill switch**; bone-conduction/thermal/GVS/tongue mostly out. Hard **safety red lines** (never chest/neck/head, pacemaker/pregnancy/epilepsy; never DIY stimulator).
- **`12_PLATFORM_AND_GENAI.md`** — save/AI-author/share haptic tracks. **White space confirmed:** MPEG-I Part 31 + RFC 9695 "MP3 of touch" finalized 2025 but **zero consumer adoption**; web haptics nearly nonexistent (iOS Safari = nothing); **no Spotify-for-haptics**. Reliable AI = **LLM→JSON score** (ChatHAP CHI 2025: Claude emits schema-valid haptic JSON). `tactus.score.json` (+ semver/parent_id) + tiered web player (vest→iPhone AHAP→Android→cymatic fallback) + 60-sec calibration + `.hjif` export.
- **Scope now 6 builds** (+ FeelStream, learned model, PlayByTouch, Claude composer, Body Synth, digital layer) + 2 stretch dimensions (multi-modal stimulation). Creative + digital deepen Anthropic/QNX/Fetch/SkyDeck — no new sponsors. New demo beats: the Body-Synth mic-drop + share-a-track. All stretches are **first on the cut list** so they never risk the core.
